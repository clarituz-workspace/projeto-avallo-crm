[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "dotnet Avallo.Web.dll --urls http://0.0.0.0:${PORT:-8080}"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
